cmake_minimum_required(VERSION 2.8)
project(conanzlib)

message(STATUS "CURRENT_OSX_VERSION ${CURRENT_OSX_VERSION}")
message(STATUS "_CURRENT_OSX_VERSION ${_CURRENT_OSX_VERSION}")
message(STATUS "CMAKE_SHARED_LIBRARY_RUNTIME_C_FLAG ${CMAKE_SHARED_LIBRARY_RUNTIME_C_FLAG}")

if(NOT "${_CURRENT_OSX_VERSION}" VERSION_LESS "10.5")
    message(STATUS "_CURRENT_OSX_VERSION >= 10.5")
else()
    message(STATUS "_CURRENT_OSX_VERSION < 10.5 :(")
endif()

message(FATAL_ERROR "abort")

