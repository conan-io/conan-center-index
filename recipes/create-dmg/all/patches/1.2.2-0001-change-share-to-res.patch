diff --git a/create-dmg b/create-dmg
index d2039bd..dc35068 100755
--- a/create-dmg
+++ b/create-dmg
@@ -320,7 +320,7 @@ else
 	# We're running inside an installed location
 	bin_dir="$SCRIPT_DIR"
 	prefix_dir=$(dirname "$bin_dir")
-	CDMG_SUPPORT_DIR="$prefix_dir/share/create-dmg/support"
+	CDMG_SUPPORT_DIR="$prefix_dir/res"
 fi
 
 if [[ -z "$VOLUME_NAME" ]]; then
