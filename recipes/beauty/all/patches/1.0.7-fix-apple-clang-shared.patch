diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 6d63dba..7ec9800 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -77,9 +77,14 @@ if(UNIX)
             UNIX
             _FORTIFY_SOURCE=2
     )
-    set_target_properties(beauty
-        PROPERTIES
-            LINK_FLAGS "-Wl,-z,relro -Wl,--as-needed"
+    target_link_options(beauty
+        PRIVATE
+            # Linux hardening / optimization
+            $<$<PLATFORM_ID:Linux>:-Wl,-z,relro>
+            $<$<PLATFORM_ID:Linux>:-Wl,--as-needed>
+            # macOS equivalents
+            $<$<PLATFORM_ID:Darwin>:-Wl,-dead_strip>
+            $<$<PLATFORM_ID:Darwin>:-Wl,-dead_strip_dylibs>
     )
 elseif(WIN32)
     target_compile_definitions(beauty
