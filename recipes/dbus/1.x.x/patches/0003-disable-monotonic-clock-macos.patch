diff --git a/meson.build b/meson.build
index d12adfac..e7ce67ec 100644
--- a/meson.build
+++ b/meson.build
@@ -328,7 +328,7 @@ data_config.set('top_builddir', meson.project_build_root())
 threads = dependency('threads')
 config.set(
     'HAVE_MONOTONIC_CLOCK',
-    cc.has_header_symbol('pthread.h', 'CLOCK_MONOTONIC', args: compile_args_c),
+    cc.has_header_symbol('pthread.h', 'CLOCK_MONOTONIC', args: compile_args_c) and not host_machine.system().contains('darwin'),
 )
 
 glib = dependency(
