diff --git a/CMakeLists.txt b/CMakeLists.txt
index 220992bc..07a2e9e6 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -79,6 +79,10 @@ if(WIN32)
     target_compile_definitions (cppserver PUBLIC -D_WIN32_WINNT=_WIN32_WINNT_WIN8)
 endif()
 
+if(CYGWIN OR MINGW)
+  add_definitions(-Wa,-mbig-obj)
+endif()
+
 # Additional module components: benchmarks, examples, plugins, tests, tools and install
 if(NOT CPPSERVER_MODULE)
 
