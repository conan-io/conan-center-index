https://github.com/yhirose/cpp-httplib/pull/656

--- a/httplib.h
+++ b/httplib.h
@@ -176,6 +176,7 @@ using socket_t = int;
 #include <array>
 #include <atomic>
 #include <cassert>
+#include <cctype>
 #include <climits>
 #include <condition_variable>
 #include <errno.h>
@@ -2838,7 +2839,7 @@ inline bool redirect(T &cli, const Request &req, Response &res,
 inline bool contains_header(const std::string &header, const std::string &name) {
   if (header.length() >= name.length()) {
     for (int i = 0; i < name.length(); ++i) {
-      if (std::tolower(header[i]) != std::tolower(name[i])) {
+      if (::tolower(header[i]) != ::tolower(name[i])) {
         return false;
       }
     }
