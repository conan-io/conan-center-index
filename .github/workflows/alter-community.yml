name: alter community

on:
  pull_request:

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/alter-community
        with:
          files: "docs/**"
          reviewers: "@RubenRBS"
